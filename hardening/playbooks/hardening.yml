---
# hardening/playbooks/hardening.yml
# Main playbook to orchestrate the security hardening roles

- name: Apply Linux Security Hardening Framework
  hosts: servers
  become: yes # Execute tasks with privilege escalation (root/sudo)
  roles:
    - ../roles/ssh_hardening
    - ../roles/firewall
